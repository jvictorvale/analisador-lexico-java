package br.com.vale;

// Enumeração com os tipos básicos exigidos
enum TokenType {
    KEYWORD, IDENTIFIER, INT_LITERAL, FLOAT_LITERAL,
    CHAR_LITERAL, STRING_LITERAL, OPERATOR, DELIMITER, ERROR, EOF
}
